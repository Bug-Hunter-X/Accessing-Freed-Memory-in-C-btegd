int main() {    int arr[5] = {1, 2, 3, 4, 5};    int *ptr = (int *)malloc(sizeof(int) * 5);    if (ptr == NULL) {        fprintf(stderr, "Memory allocation failed\n");        return 1;    }    memcpy(ptr, arr, sizeof(int) * 5);    // Access memory before freeing    for (int i = 0; i < 5; i++) {        printf("%d ", ptr[i]);    }    printf("\n");    free(ptr);    ptr = NULL; // Good practice: set pointer to NULL after freeing    // Avoid accessing freed memory    return 0;} 